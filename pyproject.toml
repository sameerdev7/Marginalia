[project]
name = "marginalia"
version = "0.1.0"
description = "A social network for readers."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "sqlalchemy>=2.0.52",
]
